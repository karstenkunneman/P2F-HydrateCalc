Web Application:
1. Prevented errors from occurring if user attempts to normalize an empty component list
2. Fixed an issue where changing input units for the minimum concentration calculation would not
   automatically convert the default values